CREATE TABLE livro (
    id INT PRIMARY KEY,
    titulo VARCHAR(255),
    autor VARCHAR(255),
    ano_publicacao VARCHAR(255),
    sinopse VARCHAR(255),
    edicao VARCHAR(255),
    editora VARCHAR(255),
    isbn INT,
    img_link VARCHAR(255)
);

